

class List2{
	static class Node{
		private int data;
		private Node next;
		
		public Node(int value) {
			data = value;
			next = null;
		}
	}
		private Node head = null;
		
		public void Display() {
			if(head == null) return;
			Node trav = head;
			
			while(trav != null) {
				System.out.print(trav.data + ", ");
				trav = trav.next;
			}
			System.out.println(" ");
		}
		
		
		public void insertSorted(int value) {
			Node newnode = new Node(value);
			
			if(head == null) {
				head = newnode;
			}
			else {
				Node trav = head;
				// if data < head.data insert at first
				if(newnode.data < head.data ) {
					newnode.next = head;
					head = newnode;
				}else if(head.next == null) {
					head.next = newnode;
				}
				else {
					while(trav.next != null && trav.next.data < value) {
						trav = trav.next;
					}
					newnode.next = trav.next;
					trav.next = newnode;
				}
			}
		}
}


public class Program2 {

	public static void main(String[] args) {
		
		List2 l2 = new List2();
		l2.insertSorted(1);
		l2.insertSorted(33);
		l2.insertSorted(-1);
		l2.insertSorted(33);
		l2.insertSorted(35);
		
		
		l2.Display();
	
	
	}

}